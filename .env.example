# Reference values for the new API. Go does not automatically load this file.
# Export variables in your shell, or configure them in your chosen process runner.
HTTP_ADDR=:8080

# TODO: Add database configuration when wiring persistence into the new API.
# The existing docker-compose.yml still uses its original development settings.

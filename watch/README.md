# Watch application

This directory is reserved for your watch client. No platform, SDK, or language
has been selected by the scaffold.

When you start, document your target platform and local setup here. Decide how
scores survive app restarts and how uploads retry after a connection failure.
Coordinate retry behavior with the API contract in `docs/api.yaml`.
